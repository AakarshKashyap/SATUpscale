# satup-setup - Lambda Dependencies
#
# IMPORTANT: PyTorch CPU wheels are ~700MB unzipped.
# Lambda zip packages are limited to 250MB unzipped.
# --> Deploy as a CONTAINER IMAGE (ECR), not a .zip
# --> See Dockerfile at project root
#
# Install locally for testing:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

# Core ML
torch==2.1.0+cpu
torchvision==0.16.0+cpu
torchsr==1.0.4

# Image processing
Pillow==10.1.0
opencv-python-headless>=4.8
numpy==1.26.2

# AWS SDK
# boto3 is pre-installed in the Lambda Python 3.11 runtime.
# Listed here only for local development/testing.
boto3==1.34.0
